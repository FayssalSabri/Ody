name = "odyssey-backend"
main = "src/worker.ts"
compatibility_date = "2024-11-12"
compatibility_flags = ["nodejs_compat"]

[vars]
SERVICE_NAME = "odyssey-backend"

# Set DATABASE_URL via `wrangler secret put DATABASE_URL` for deployed environments.
# Local Workers dev can use `.dev.vars` (see `.dev.vars.example`).
